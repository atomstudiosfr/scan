name: Rabbit

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
    paths:
      - 'backend/docker/rabbit.Dockerfile'
      - 'kubernetes/rabbit-**.yaml'
      - '.github/workflows/rabbit.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Login to Scaleway Container Registry
      uses: docker/login-action@v3
      with:
        username: nologin
        password: ${{ secrets.SCALEWAY_API_KEY }}
        registry: rg.fr-par.scw.cloud/atom-namespace
    - name: Build the Docker image
      run: docker build . -f ./backend/docker/rabbit.Dockerfile -t rg.fr-par.scw.cloud/atom-namespace/apps/scan-rabbit
    - name: Push the Docker Image
      run: docker push rg.fr-par.scw.cloud/atom-namespace/apps/scan-rabbit
